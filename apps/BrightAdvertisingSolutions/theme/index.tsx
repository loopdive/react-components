import { css } from "styled-components";
import { input, searchBox } from "./input";

export const TopBarStyle = css`
  height: 10rem;
  display: flex;
  align-items: center;
  justify-content: center;
`;

export default {
  input,
  searchBox,
  TopBarStyle
};
